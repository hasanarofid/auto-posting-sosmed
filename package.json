{
  "name": "sosmed-auto-poster",
  "version": "1.0.0",
  "description": "Sistem upload konten otomatis ke Instagram, LinkedIn, Threads, dan TikTok",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && nodemon index.js",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["social-media", "automation", "instagram", "linkedin", "threads", "tiktok"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

